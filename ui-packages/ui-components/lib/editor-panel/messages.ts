import { defineMessages } from 'react-intl';

export const messages = defineMessages({
  splitViewButton: {
    id: 'editor-panel.split-view-button',
    defaultMessage: 'Split view',
    description: 'Button tooltip to toggle split editor view',
  },
});
